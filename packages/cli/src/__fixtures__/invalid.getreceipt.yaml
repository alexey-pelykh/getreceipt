# Structurally invalid: `kind: magic` is not a recognized auth kind, so
# ConfigLoader (#6) throws a path-named ConfigError — exercised by `validate`.
profiles:
  default:
    sources:
      free.fr:
        auth:
          kind: magic
