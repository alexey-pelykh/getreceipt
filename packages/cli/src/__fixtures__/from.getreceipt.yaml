# Flat config exercised by the `from`-command tests (the "default" profile). `shop.example` matches
# the fake adapter's canonical domain; the inline secrets are obvious non-secrets (the #7 clean-tree
# scan ignores them) and the tests inject the credential resolver anyway.
sources:
  shop.example:
    auth:
      kind: password
      username: alice@shop.example
      secret: hunter2-not-real
